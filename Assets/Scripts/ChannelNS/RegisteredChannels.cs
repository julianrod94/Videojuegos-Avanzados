namespace ChannelNS {
    public enum RegisteredChannels {
        PlayerPositionChannel = 1,
        PlayerInputChannel,
        OtherPlayersChannel,
        PlayerEventChannel,
        GrenadeStatesChannel
    }
}
